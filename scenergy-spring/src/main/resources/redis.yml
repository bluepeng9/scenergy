spring:
  config:
    activate:
      on-profile: test
  redis:
    host: localhost
    port: 6379
---
spring:
  config:
    activate:
      on-profile: dev
  redis:
    host: localhost
    port: 6379
---
spring:
  config:
    activate:
      on-profile: prod
  redis:
    host: localhost
    port: 6379
